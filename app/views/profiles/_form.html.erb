<%= form_with(model: profile) do |profile_form| %>
  <% if @user.profile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@user.profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
        <% @user.profile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= profile_form.label :pfp %>
    <%= profile_form.file_field :pfp %>
  </div>

  <div>
    <%= profile_form.label :banner %>
    <%= profile_form.file_field :banner %>
  </div>

  <div>
    <%= profile_form.label :bio, style: "display: block" %>
    <%= profile_form.text_area :bio %>
  </div>

  <div>
    <%= profile_form.submit %>
  </div>
<% end %>
