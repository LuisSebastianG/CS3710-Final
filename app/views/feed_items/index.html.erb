<p id="notice"><%= notice %></p>

<h1>Feed Items</h1>

<table>
  <thead>
    <tr>
      <th>Body</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @feed_items.each do |feed_item| %>
      <tr>
        <!-- Feed item body -->
        <td><%= feed_item.body %></td>

        <!-- Associated user name -->
        <td><%= feed_item.user.username %></td>

        <!-- Links for actions -->
        <td><%= link_to 'Show User', [@user] %></td>
        <td><%= link_to 'Edit Feed Item', edit_user_feed_item_path(@user, feed_item) %></td>
        <td><%= button_to 'Destroy Feed Item', [@user, feed_item], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Show User', [@user] %>
<%= link_to 'New Feed Item', new_user_feed_item_path(@user) %>
